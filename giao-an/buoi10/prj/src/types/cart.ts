export interface CartItem {
    id: number
    name: string
    price: number
    quantity: number
    img: string
}

export interface Cart {
    id: number
    name: string
    price: number
    img: string
}
